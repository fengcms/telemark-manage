export type {
  ApiErrorBody,
  CustomerStatus,
  CustomerType,
  PageResponse as ApiListResponse,
  Role as UserRole,
} from '@/api/types';

export { customerStatuses, customerTypes, roles as userRoles } from '@/lib/constants';
